# Jávea Real Estate - Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# SUPABASE (Required for production)
# =============================================================================
# Get these from your Supabase project dashboard: https://app.supabase.com
# Settings → API → Project URL and Project API keys

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# Your Supabase anonymous/public key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxx

# Service role key (NEVER expose this in client code - server-side only)
# Only needed for admin operations, scraper, or server-side functions
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxx

# =============================================================================
# AUTHENTICATION (NextAuth.js with Google OAuth)
# =============================================================================
# Get Google OAuth credentials from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID → Web application
# Add authorized redirect URI: http://localhost:3000/api/auth/callback/google

# NextAuth base URL (use your production URL when deploying)
NEXTAUTH_URL=http://localhost:3000

# NextAuth secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your-secret-key-change-this-in-production

# Google OAuth Client ID
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth Client Secret
GOOGLE_CLIENT_SECRET=GOCSPX-your-google-client-secret

# =============================================================================
# GOOGLE MAPS (Optional - for geocoding and map features)
# =============================================================================
# Get API key from: https://console.cloud.google.com/apis/credentials
# Enable: Geocoding API, Maps JavaScript API

NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# =============================================================================
# EMAIL (Optional - for alerts and notifications)
# =============================================================================
# Resend: https://resend.com (recommended - free tier available)
# SendGrid: https://sendgrid.com

RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXX

# From email address (must be verified domain in Resend/SendGrid)
EMAIL_FROM=noreply@javearealestate.com

# =============================================================================
# ANALYTICS (Optional - for tracking)
# =============================================================================

# Google Analytics 4 Measurement ID
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Plausible (privacy-friendly alternative)
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=javearealestate.com

# =============================================================================
# ERROR TRACKING (Optional - for production monitoring)
# =============================================================================

# Sentry DSN
NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx

# =============================================================================
# SCRAPER (For web scraping service)
# =============================================================================

# Proxy service (optional but recommended to avoid IP bans)
# Bright Data, Oxylabs, or similar
PROXY_URL=http://username:password@proxy.example.com:8080

# Geocoding service (if not using Google Maps)
# Nominatim (free): https://nominatim.org/
GEOCODING_API_URL=https://nominatim.openstreetmap.org

# =============================================================================
# DEVELOPMENT
# =============================================================================

# Set to 'development' or 'production'
NODE_ENV=development

# Next.js build ID (auto-generated, don't set manually)
# NEXT_BUILD_ID=

# =============================================================================
# NOTES
# =============================================================================
#
# 1. NEVER commit .env.local to git (already in .gitignore)
# 2. For Vercel deployment, add env vars in dashboard: Settings → Environment Variables
# 3. For local development, copy this to .env.local and fill in values
# 4. Supabase keys are required; everything else is optional for basic functionality
# 5. Use NEXT_PUBLIC_ prefix for variables that need to be exposed to the browser
# 6. Keep SUPABASE_SERVICE_ROLE_KEY secret - it bypasses Row Level Security!
